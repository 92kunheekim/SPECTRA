"""Two-stage Rosetta multi-task pretraining (dG regression + binding BCE).

Reference implementation: experiments/model_generation/pretrain_rosetta.py.
It targets the `model_llm` generation of the architecture; porting it to the
flagship EGNN model (spectra.models.spectra_model) is tracked in CHANGELOG.
"""
